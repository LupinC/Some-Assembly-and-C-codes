#include<stdio.h>

int main(int argc, char const *argv[])
{
    
    printf("%d\n", sizeof(int));

    //d->sign, decimal integer; lu-> unsigned integer
    return 0;
    //long long ll
    //sizeof(ll) -> 8
    //unsigned long ul = sizeof(ll)
    //when compiling, no error showing up
    //every function can only be called after declare

    //int unsigned floating: the only 3 primitive data type in c

    //casting
    //int x = 4.5;
    //it will authomatically convert it to 4

}

//I will get a warning when I compile
//gives a long unsigned int
//when recompile, the warning will be gone